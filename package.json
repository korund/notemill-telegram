{
  "name": "notes-capture-bot",
  "version": "0.0.1",
  "private": true,
  "description": "Telegram-side stub producer for the voice2text worker (Slice 1: no Telegram yet).",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "produce": "node --experimental-sqlite --import tsx src/bin/produce.ts",
    "poll": "node --experimental-sqlite --import tsx src/bin/poll.ts"
  },
  "dependencies": {
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
